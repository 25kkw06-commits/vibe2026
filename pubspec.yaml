name: app_usage_tracker
description: 안드로이드 앱 사용시간 추적 및 제한 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  usage_stats: ^1.4.0
  installed_apps: ^1.5.2
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.2.2
  permission_handler: ^11.3.1
  workmanager: ^0.5.2
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
